"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { FormEvent, useState } from "react";
import { ArrowRight, Loader2, ShieldCheck } from "lucide-react";
import { apiClient } from "@/lib/api";
import { AuthUser, saveAuth } from "@/lib/auth";
import { useLanguage } from "@/context/LanguageContext";

export default function RegisterPage() {
  const router = useRouter(); const { language } = useLanguage(); const vi = language === "vi";
  const [form, setForm] = useState({ username: "", phone: "", password: "", birthday: "" });
  const [loading, setLoading] = useState(false); const [error, setError] = useState("");
  const update = (key: keyof typeof form) => (event: React.ChangeEvent<HTMLInputElement>) => setForm({ ...form, [key]: event.target.value });
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setError(""); setLoading(true); try {
    await apiClient.post("/api/v1/news-feed/users", form);
    const login = await apiClient.post<{ data: { token: string } }>("/api/v1/news-feed/auth/login", { phone: form.phone, password: form.password });
    const token = login.data.data.token;
    const me = await apiClient.get<{ data: AuthUser }>("/api/v1/news-feed/users/myinfo", { headers: { Authorization: `Bearer ${token}` } });
    saveAuth(token, me.data.data); router.replace("/");
  } catch (err: any) { setError(err?.response?.data?.message || (vi ? "Không thể tạo tài khoản. Hãy kiểm tra lại thông tin." : "We could not create the account. Please check your details.")); } finally { setLoading(false); } }
  return <main className="min-h-[calc(100vh-12rem)] px-4 py-12 sm:py-20"><div className="mx-auto grid max-w-5xl overflow-hidden rounded-2xl border border-slate-800 bg-slate-950/70 shadow-2xl shadow-sky-950/20 lg:grid-cols-[1fr_0.9fr]"><section className="hidden border-r border-slate-800 bg-[radial-gradient(circle_at_25%_25%,rgba(14,165,233,0.18),transparent_45%)] p-12 lg:block"><div className="flex items-center gap-2 text-sky-300"><ShieldCheck className="h-6 w-6" /><span className="font-semibold">CryptoCheck</span></div><h1 className="mt-16 text-4xl font-semibold leading-tight text-white">{vi ? "Một tài khoản cho mọi quyết định crypto." : "One account for every crypto decision."}</h1><p className="mt-5 max-w-sm text-sm leading-6 text-slate-400">{vi ? "Lưu danh sách theo dõi, mở khóa các tính năng cộng đồng và quay lại các công cụ bạn cần." : "Save watchlists, unlock community features and return to the tools you need."}</p></section><section className="p-6 sm:p-10"><div className="mb-8"><div className="eyebrow">CryptoCheck account</div><h1 className="mt-2 text-3xl font-semibold text-white">{vi ? "Tạo tài khoản" : "Create your account"}</h1><p className="mt-2 text-sm text-slate-400">{vi ? "Mất chưa đến một phút để bắt đầu." : "It takes less than a minute to get started."}</p></div><form className="grid gap-4 sm:grid-cols-2" onSubmit={submit}><label className="block text-sm font-medium text-slate-300 sm:col-span-2">{vi ? "Tên hiển thị" : "Display name"}<input required value={form.username} onChange={update("username")} className="mt-2 w-full rounded-xl border border-slate-800 bg-slate-900 px-4 py-3 text-white outline-none focus:border-sky-400" /></label><label className="block text-sm font-medium text-slate-300">{vi ? "Số điện thoại" : "Phone number"}<input required inputMode="numeric" value={form.phone} onChange={update("phone")} placeholder="0901234567" className="mt-2 w-full rounded-xl border border-slate-800 bg-slate-900 px-4 py-3 text-white outline-none focus:border-sky-400" /></label><label className="block text-sm font-medium text-slate-300">{vi ? "Ngày sinh" : "Birthday"}<input required type="date" value={form.birthday} onChange={update("birthday")} className="mt-2 w-full rounded-xl border border-slate-800 bg-slate-900 px-4 py-3 text-white outline-none focus:border-sky-400" /></label><label className="block text-sm font-medium text-slate-300 sm:col-span-2">{vi ? "Mật khẩu" : "Password"}<input required minLength={6} type="password" value={form.password} onChange={update("password")} className="mt-2 w-full rounded-xl border border-slate-800 bg-slate-900 px-4 py-3 text-white outline-none focus:border-sky-400" /></label>{error && <p role="alert" className="rounded-lg border border-red-500/30 bg-red-500/10 p-3 text-sm text-red-200 sm:col-span-2">{error}</p>}<button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-xl bg-sky-500 px-4 py-3 font-semibold text-slate-950 transition hover:bg-sky-400 disabled:opacity-60 sm:col-span-2">{loading ? <Loader2 className="h-4 w-4 animate-spin" /> : <>{vi ? "Tạo tài khoản" : "Create account"}<ArrowRight className="h-4 w-4" /></>}</button></form><p className="mt-6 text-center text-sm text-slate-400">{vi ? "Đã có tài khoản?" : "Already registered?"} <Link href="/login" className="font-semibold text-sky-300 hover:text-sky-200">{vi ? "Đăng nhập" : "Sign in"}</Link></p></section></div></main>;
}
